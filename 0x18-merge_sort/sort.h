#ifndef MERGE_H
#define MERGE_H

#include <stdio.h>
#include <stdlib.h>

void print_array(const int *array, size_t size);
void merge_sort(int *array, size_t size);
void merge(int *A, int *left, int *, size_t size);

#endif /* Merge sort Algorithm */
